---
- name: "Configure the EKS cluster with kubectl"
  hosts: management
  user: ec2-user
  gather_facts: false
  become: yes
  vars:
    ENVIRONMENT_NAME: "{{ lookup('env', 'ENVIRONMENT_NAME') }}"
    ENVIRONMENT_NAME: "{{ lookup('env', 'CIRCLE_WORKFLOW_ID') }}"
  roles:
    - deployment
    #- services



