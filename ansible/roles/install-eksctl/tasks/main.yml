---
- name: "Install tar"
  shell: yum install tar
- name: "Get eksctl bin"
  shell: curl --silent --location "https://github.com/weaveworks/eksctl/releases/tag/v0.131.0/eksctl_$(uname -s)_amd64.tar.gz" | tar xz -C /tmp
- name: "Copy bin"
  shell: mv /tmp/eksctl /usr/local/bin

